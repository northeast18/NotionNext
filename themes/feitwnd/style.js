/* eslint-disable react/no-unknown-property */
export const Style = () => {
    return (
        <style jsx global>{`
      /* 自定义CSS样式，此处可引入参考网站的特色样式 */
      #theme-feitwnd {
        font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      }
    `}</style>
    )
}
